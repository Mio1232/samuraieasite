# 記事管理画面（/admin）の使い方

このサイトには、ブラウザでプレゼント記事を作成・編集・削除できる管理画面（Decap CMS）を組み込みました。
`サイトのURL/admin`（例：`https://あなたのサイト.netlify.app/admin`）にアクセスして使います。

無料で使えますが、「保存＝公開サイトの書き換え」になるため、一度だけログイン設定が必要です（あなた1人だけ招待します）。

---

## 初回だけ：ログインを使えるようにする（Netlify側の設定）

1. Netlify の管理画面で対象サイトを開く。
2. 左メニュー **Identity** →「Enable Identity」。
3. Identity 設定内の **Registration** を「Invite only」にする（他人が勝手に登録できないように）。
4. 同じく Identity の **Services → Git Gateway** を「Enable Git Gateway」。
5. **Identity → Invite users** で、自分のメールアドレスを招待。
6. 届いた招待メールのリンクを開き、パスワードを設定。
7. 以降は `サイトのURL/admin` でメール＋パスワードでログインできます。

> ブランチ名の注意：`admin/config.yml` の `branch:` は、GitHubリポジトリの既定ブランチ名（`main` か `master`）に合わせてください。今は `main` になっています。違う場合はここだけ直してください。

---

## 記事を作る・直す・消す

1. `サイトのURL/admin` を開いてログイン。
2. 「プレゼント記事」→「プレゼント記事（一覧をまとめて管理）」を開く。
3. 「記事」リストで操作します：
   - **追加**：＋ボタンで新しい記事を追加。
   - **編集**：既存の記事を開いて書き換え。
   - **削除**：記事の menu から削除。
   - **並び替え**：ドラッグで順番を変更（上ほど新しく表示されます。日付でも自動整列します）。
4. 各記事の入力項目：
   - **公開する**：オフにすると下書き（サイトに出ません）。
   - **スラッグ**：記事URLになる半角英数字（例 `2026-09`）。記事ごとに重複しない値に。
   - **タイトル / 日付 / ステータス（開催中・終了）**。
   - **要約**：一覧カードに出る短い説明。
   - **絵文字 / ヒーロー画像**：一覧サムネイル。画像はドラッグでアップできます。
   - **オープンチャットURL**：受け取りボタン用（空なら非表示）。
   - **本文**：見出し・太字・画像・箇条書き・引用などが使えます（下記）。
   - **記事下にEA紹介を表示**：samurai / ムサシ の紹介カードの表示オンオフ。
5. 「Publish（公開）」を押すと、数十秒〜数分でサイトに反映されます。

---

## 本文の書き方（Markdown）

管理画面の本文エディタは、ボタンで見出し・太字・画像などを入れられます。手打ちする場合の記法：

- 見出し：`## 大見出し` / `### 小見出し`
- 太字：`**太字**`　斜体：`*斜体*`　マーカー：`==強調==`
- 箇条書き：行頭に `- `　番号付き：`1. `
- 引用：行頭に `> `
- 区切り線：`---`
- リンク：`[表示文字](https://…)`
- 画像：`![説明](画像のURL)`　※エディタの画像ボタンでアップすると自動で入ります。画像はタップで全画面拡大されます。

---

## 注意（景表法）

プレゼントは「オープンチャット参加者向け」の参加型にしてください。
「口座開設したら」「EA購入で」など、取引を条件にしたプレゼントは避けてください。
